.hero {
  height: 620px;
  background-image: url(../images/duck.png);
  background-repeat: no-repeat;
  background-position: center;
  background-size: auto;
  position: relative; /* Добавляем для корректного позиционирования детей */
}

.hero__container {
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center; /* Центрируем контент по вертикали */
}

.hero__title {
  font-family: var(--second-family);
  font-weight: 700;
  font-size: 152px;
  line-height: 100%;
  text-transform: uppercase;
  color: #1e1e1e;
  display: flex;
  justify-content: space-between;
  align-items: flex-start; /* Выравниваем по верху */
}

.hero__title-left {
  display: flex;
  align-items: center; /* Выравниваем текст и SVG по центру вертикально */
  position: relative;
}

.hero__img {
  position: relative; /* Меняем absolute на relative */
  top: auto;
  left: auto;
  margin-left: 20px; /* Отступ между текстом и SVG */
}

.hero__title-right {
  display: flex;
  flex-direction: column;
  align-items: flex-start; /* Выравниваем по левому краю */
  gap: 20px; /* Отступ между элементами */
}

.hero__wrapper {
  width: 315px;
  font-family: var(--font-family);
  font-weight: 400;
  font-size: 16px;
  line-height: 125%;
  letter-spacing: 0.01em;
  color: #1f2937;
  margin-top: 20px; /* Отступ от заголовка */
}

.hero__btn {
  border-radius: 8px;
  padding: 10px 12px;
  background: var(--blue);
  font-family: var(--font-family);
  font-weight: 600;
  font-size: 15px;
  letter-spacing: 0.01em;
  text-align: center;
  color: #fff;
  margin-top: 30px; /* Отступ от текста */
  align-self: flex-start; /* Выравниваем кнопку по левому краю */
}